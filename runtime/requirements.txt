# Runtime vocal auto-hébergé — Python 3.11+
# STT (faster-whisper) tourne en processus : première exécution = téléchargement
# des modèles (~1,5 Go pour whisper medium).
#
# Pas d'extra `piper` ici, volontairement. Il tire `piper-tts`, GPL-3.0-or-later,
# et `pipecat.services.piper.tts` l'importe au niveau du module — installer
# l'extra chargerait donc du code GPL dans ce processus, y compris pour la
# variante HTTP de Pipecat. Le runtime parle au serveur Piper en HTTP
# (piper_http.py), l'opérateur lance ce serveur à part (voir README).
# La CI vérifie que piper-tts reste hors de l'arbre de dépendances.
pipecat-ai[silero,whisper,anthropic,openai]>=1.5,<2
aiohttp>=3.9
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
loguru>=0.7
